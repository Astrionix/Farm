import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mahalakshmi.poultryerp',
  appName: 'SMP Poultry ERP',
  webDir: 'out'
};

export default config;
